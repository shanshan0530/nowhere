[project]
name = "nowhere-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastmcp>=2.11,<4", "httpx", "numpy", "scipy", "ephem", "skyfield", "timezonefinder", "tzdata", "uvicorn", "opencc-python-reimplemented"]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "respx"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
